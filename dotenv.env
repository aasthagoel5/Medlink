# ----------------------------------------------------------------------
# Medlink Environment Variables (.env)
# ----------------------------------------------------------------------

# --- 1. Database Configuration (CRITICAL for server startup) ---
# You MUST replace the values below with the credentials for your MySQL database.
DB_HOST=localhost
DB_USER=root                    # Your MySQL username (e.g., 'root')
DB_PASSWORD=123456 # The password for the user above
DB_DATABASE=health_records          # The name of your database (e.g., 'medlink_db')

# --- 2. Application Secrets ---
# JWT Secret for creating authentication tokens.
JWT_SECRET="TThisIsAComplexAndLongStringThatShouldBeKeptSecret1234567890"

# --- 3. Email Service Configuration (for OTP) ---
# Use a Gmail account and generate a specific App Password for this to work.
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=YOUR_GMAIL_APP_PASSWORD

# --- 4. Server Settings ---
PORT=3000
FRONTEND_URL=http://localhost:3000 # Use * if you are running frontend from a different port, but this is safer.